{
  "name": "memex",
  "version": "1.0.0",
  "description": "Poovi's Second Brain — AI-maintained knowledge base",
  "scripts": {
    "build": "quartz build",
    "dev": "quartz build --serve",
    "preview": "quartz build --serve"
  },
  "dependencies": {
    "@jackyzha0/quartz": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
